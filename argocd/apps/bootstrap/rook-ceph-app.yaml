apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rook-ceph
  namespace: argocd
spec:
  project: project-bootstrap
  sources:
    - repoURL: https://charts.rook.io/release 
      chart: rook-ceph
      targetRevision: 1.17.4
      helm:
        releaseName: rook-ceph
        valueFiles:
          - $values/argocd/apps/bootstrap/values/rook-ceph-values.yaml
    - repoURL: https://github.com/aryatavakoli/k8-homelab-charts
      targetRevision: HEAD
      ref: values
  destination:
    server: "https://kubernetes.default.svc"
    namespace: rook-ceph
  syncPolicy:
    managedNamespaceMetadata:
      labels: 
        pod-security.kubernetes.io/enforce: privileged
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

