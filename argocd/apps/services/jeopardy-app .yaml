apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jeopardy-app 
  namespace: argocd
spec:
  project: project-services
  sources:
    - repoURL: https://github.com/aryatavakoli/k8-homelab-charts
      targetRevision: HEAD
      path: argocd/apps/services/manifests/jeopardy-app
  destination:
    server: "https://kubernetes.default.svc"
    namespace: jeopardy-app
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true