apiVersion: v1
kind: Service
metadata:
  name: minekube-gate
  namespace: minecraft
spec:
  ports:
  - name: minecraft
    port: 25565
    protocol: TCP
    targetPort: minecraft
  selector:
    app: minekube-gate