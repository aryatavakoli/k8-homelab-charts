apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: minekube-gate
  namespace: minecraft
spec:
  parentRefs:
  - name: tcp-gateway
    sectionName: minecraft-gateway
  rules:
  - backendRefs:
    - name: minekube-gate
      port: 25565